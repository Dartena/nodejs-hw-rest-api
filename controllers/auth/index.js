const currentUser = require("./current");
const loginUser = require("./login");
const signupUser = require("./signup");
const logoutUser = require("./logout");

module.exports = { currentUser, loginUser, signupUser, logoutUser };
